/*
 * Login
 * Ext.form.Panel
 */
Ext.define('skin.view.Login', {
  extend: 'Ext.form.Panel',
  alias: 'widget.loginview',
  config: {
    title: 'Login',
    flex: 2,
    items: [],// eof items
  }// eof config
});